package dev.slate.ai.download.engine

import javax.inject.Inject
import javax.inject.Singleton

/**
 * Coordinates model downloads via WorkManager.
 * Full implementation will be added in Phase 4.
 */
@Singleton
class ModelDownloadManager @Inject constructor() {
    // Phase 4: WorkManager enqueue, progress observation, cancel, resume
}
